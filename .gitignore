# API keys — never commit
Code/comptox_api_key.txt
# Google service account key (any JSON in Data/ or Code/ that looks like a key)
Code/taxotox-service.json

*-[0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f].json

# Local install state — machine-specific, not shared
Data/install_timestamps.csv
Data/dtxsid_map.csv

# Large data files — stored locally, not in repo
Data/final_ecotox_data.fst
Data/taxotox_reference.fst
Data/Known_CAS.fst
Data/Known_CAS_backup.fst

# R session artefacts
.Rhistory
.RData
.Rproj.user/
.Rprofile

# IDE and tool configuration — local only
.claude/
.vscode/
.gitattributes

# ShinyApps deployment state — machine-specific
rsconnect/

# Orphaned htmlwidget dependency bundle (no HTML report references it anymore)
Docs/lib/

# Office lock/temp files
~$*

# Dev-only QA fixture, not a curated user-facing sample
Data/sample_coverage_test.csv

# Local Shiny run logs — machine-specific, not shared
logs/

# Raw screencast recordings — uploaded to YouTube (unlisted) and linked from
# User_Guide.md instead of committed; keeps the repo small for paper readers.
Docs/screencasts/

# Reference literature / package documentation PDFs — kept for local
# reading, not for redistribution via the repo.
Docs/Backhaus, Faust - 2012 - Predictive environmental risk assessment of chemical mixtures A conceptual framework.pdf
Docs/ECOTOXr.pdf
Docs/ECOTOXr_p.pdf

# Ad-hoc analysis scratch outputs — not curated user-facing docs
Docs/nowell_imbl_fix_comparison.csv
Docs/nowell_vs_median_ratios.csv
Docs/TaxoTox_output_template.xlsx
